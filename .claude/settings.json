{
  "mcpServers": {
    "email": {
      "type": "stdio",
      "command": "python",
      "args": ["email_mcp_server.py"],
      "description": "Email tool — sends emails via AWS SES. Exposes send_email(to_email, from_email, subject, content). Both addresses must be SES-verified."
    }
  }
}
